Boot thing (Floppy disk, boot partition, etc.):
    Sector 1 (Byte 1 - 512):
        first-stage (wintshBL-core/wintshBL-core.asm)
    Sector 2 - X (Byte 513 - X (Multiple of 512))
        second-stage ()
